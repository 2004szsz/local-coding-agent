---
description: 每次改完代码必须提交并推送到 GitHub；仓库 Git 配置以 .gitconfig 为准
alwaysApply: true
---

# Git 提交与推送（强制）

## 仓库

- 远程：`https://github.com/2004szsz/local-coding-agent.git`
- 默认分支：`main`
- 可提交的 Git 配置镜像：仓库根目录 `.gitconfig`（remote / branch 等）

## 为何不是 `.git/config`

Git **不能**把工作区内的 `.git/` 目录加入版本库（那是仓库元数据本身）。  
因此本项目用根目录 **`.gitconfig`** 保存并提交与 `.git/config` 等价的远程/分支配置。  
若本机 remote 或默认分支有变更，必须同步更新 `.gitconfig` 并一并提交推送。

## 每次改完代码必须执行

凡在本项目中完成**代码修改、功能新增、配置/规则更新**后，在收尾阶段**必须**执行（除非用户明确说「不要提交」）：

1. `git status` / `git diff` 确认变更；勿提交 `.env`、密钥、`.venv/`、`data/` 等
2. `git add` 相关文件（含本次改动的 `.gitconfig`、`.cursor/rules/*` 若有）
3. `git commit`（简明说明 why）
4. `git push -u origin HEAD`（或当前分支已跟踪则 `git push`）

目标：GitHub 上的 `2004szsz/local-coding-agent` 始终包含最新代码与完整提交历史。

## 禁止

- 改完代码只留在本地、不推送
- 提交 `.env` 或含真实 API Key 的文件
- 用 `--force` 推送 `main`（除非用户明确要求）
