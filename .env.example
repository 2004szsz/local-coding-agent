# shellcheck shell=sh disable=SC2148
# 复制为 .env 后填写。进程环境变量优先于本文件，本文件优先于 config.yaml 里的同名字段。
# 不要把真实 Key 写进 config.yaml 或提交到仓库。
#
# 模型供应商与 API Key 请在 Web 界面「设置 → 模型设置」中配置（data/model_registry.json）。
# 下方 LLM_* 仅作无注册表时的兜底，一般无需填写。

# 对话模型（OpenAI 兼容，可选兜底）
# LLM_API_KEY=
# LLM_BASE_URL=
# LLM_MODEL=
# LLM_PROVIDER=custom

# RAG 嵌入（可选兜底）
# EMBEDDING_API_KEY=
# EMBEDDING_BASE_URL=
# EMBEDDING_MODEL=

# 开启后，每次工具调用追加到 data/logs/agent.log。python run.py --debug 或 --env dev 也会打开。
# AGENT_DEBUG=1

# 临时覆盖 agents/agent.yaml 的框架：langgraph | native_react | autogen | llamaindex | crewai
# AGENT_FRAMEWORK=native_react
